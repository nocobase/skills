blocks:
  - key: filterForm
    type: filterForm
    coll: nb_pm_tasks
    fields:
      - field: name
        label: Search
        filterPaths:
          - name
          - description
          - project.name
          - assignee.name
      - status
      - priority
    js_items:
      - key: filter_buttons
        file: ./js/filter_buttons.js
        desc: Task Filter Buttons
    field_layout:
      - - '[JS:Task Filter Buttons]'
      - - name
        - status
        - priority
  - key: table
    type: table
    coll: nb_pm_tasks
    fields:
      - field: name
        popup: true
      - status
      - priority
      - project
      - assignee
      - dueDate
      - createdAt
    actions:
      - filter
      - refresh
      - addNew
    recordActions:
      - edit
      - delete
layout:
  - - filterForm
  - - table
