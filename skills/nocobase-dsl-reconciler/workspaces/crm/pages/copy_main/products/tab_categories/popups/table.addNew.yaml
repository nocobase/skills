target: $SELF.table.actions.addNew
mode: drawer
blocks:
  - key: createForm
    type: createForm
    coll: nb_crm_product_categories_copy
    fields:
      - description
      - sort_order
      - name
    field_layout:
      - - description
      - - sort_order
      - - name
    actions:
      - submit
