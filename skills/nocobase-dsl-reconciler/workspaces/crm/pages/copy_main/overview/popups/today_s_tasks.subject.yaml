target: $SELF.today_s_tasks.fields.subject
mode: drawer
blocks:
  - key: details
    type: details
    coll: nb_crm_activities_copy
    resource_binding:
      filterByTk: '{{ctx.view.inputArgs.filterByTk}}'
    fields:
      - createdAt
    field_layout:
      - - createdAt
