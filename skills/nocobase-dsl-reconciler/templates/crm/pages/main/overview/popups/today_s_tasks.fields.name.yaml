target: $SELF.today_s_tasks.fields.name
coll: ''
blocks:
  - key: details
    type: details
    coll: ''
    templateRef:
      templateUid: fcek1z711h6
      templateName: 'Form (Add new): Activities'
      targetUid: fkdhtbsahxe
      mode: reference
    resource_binding:
      filterByTk: '{{ctx.view.inputArgs.filterByTk}}'
    fields:
      - createdAt
mode: drawer
