target: $SELF.today_s_tasks.recordActions.edit
coll: ''
blocks:
  - key: editForm
    type: editForm
    coll: ''
    actions:
      - submit
      - type: ai
        employee: dex
    templateRef:
      templateUid: fcek1z711h6
      templateName: 'Form (Add new): Activities'
      targetUid: fkdhtbsahxe
      mode: reference
    resource_binding:
      filterByTk: '{{ctx.view.inputArgs.filterByTk}}'
