target: $SELF.table.recordActions.edit
coll: ''
blocks:
  - key: editForm
    type: editForm
    coll: ''
    fields:
      - product
      - purchase_order
      - quantity
      - unit_price
    field_layout:
      - - product
        - purchase_order
      - - quantity
        - unit_price
    actions:
      - submit
    resource_binding:
      filterByTk: '{{ctx.view.inputArgs.filterByTk}}'
