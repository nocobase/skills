target: $SELF.table.fields.name
coll: ''
blocks:
  - key: details
    type: details
    coll: ''
    fields:
      - product
      - purchase_order
      - quantity
      - unit_price
      - createdAt
    field_layout:
      - - product
        - purchase_order
      - - quantity
        - unit_price
    resource_binding:
      filterByTk: '{{ctx.view.inputArgs.filterByTk}}'
mode: drawer
