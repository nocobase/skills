target: $SELF.table.fields.name
coll: ''
blocks:
  - key: details
    type: details
    coll: ''
    fields:
      - code
      - supplier
      - order_date
      - total_amount
      - status
      - createdAt
    field_layout:
      - '--- Order Info ---'
      - - code
        - status
      - - supplier
        - order_date
      - '--- Amount ---'
      - - total_amount
    resource_binding:
      filterByTk: '{{ctx.view.inputArgs.filterByTk}}'
mode: drawer
