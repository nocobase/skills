target: $SELF.table.recordActions.edit
coll: ''
blocks:
  - key: editForm
    type: editForm
    coll: ''
    fields:
      - code
      - supplier
      - order_date
      - total_amount
      - status
    field_layout:
      - '--- Order Info ---'
      - - code
        - status
      - - supplier
        - order_date
      - '--- Amount ---'
      - - total_amount
    actions:
      - submit
    resource_binding:
      filterByTk: '{{ctx.view.inputArgs.filterByTk}}'
