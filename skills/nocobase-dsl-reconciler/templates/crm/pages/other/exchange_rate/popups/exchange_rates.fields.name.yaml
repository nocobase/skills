target: $SELF.exchange_rates.fields.name
coll: ''
blocks:
  - key: details
    type: details
    coll: ''
    fields:
      - rate_to_usd
      - currency
      - effective_date
      - source
      - createdAt
    field_layout:
      - - rate_to_usd
      - - currency
      - - effective_date
      - - source
    resource_binding:
      filterByTk: '{{ctx.view.inputArgs.filterByTk}}'
mode: drawer
