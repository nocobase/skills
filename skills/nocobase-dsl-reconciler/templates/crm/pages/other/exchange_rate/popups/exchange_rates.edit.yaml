target: $SELF.exchange_rates.recordActions.edit
coll: ''
blocks:
  - key: editForm
    type: editForm
    coll: ''
    fields:
      - rate_to_usd
      - currency
      - effective_date
      - source
    field_layout:
      - - rate_to_usd
      - - currency
      - - effective_date
      - - source
    actions:
      - submit
    resource_binding:
      filterByTk: '{{ctx.view.inputArgs.filterByTk}}'
