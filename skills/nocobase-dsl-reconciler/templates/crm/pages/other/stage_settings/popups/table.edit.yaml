target: $SELF.table.recordActions.edit
coll: ''
blocks:
  - key: editForm
    type: editForm
    coll: ''
    fields:
      - name
      - win_probability
      - color
      - description
      - code
    field_layout:
      - - name
        - code
      - - win_probability
      - - color
      - - description
    actions:
      - submit
    resource_binding:
      filterByTk: '{{ctx.view.inputArgs.filterByTk}}'
