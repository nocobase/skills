const r=ctx.record||{};const target=Number(r.{TARGET})||0;const actual=Number(r.{ACTUAL})||0;const pct=target>0?Math.round(actual/target*100):0;const color=pct>=100?'#52c41a':pct>=80?'#1890ff':pct>=60?'#faad14':'#cf1322';const h=ctx.React.createElement;const fmt=v=>v>=10000?'¥'+(v/10000).toFixed(1)+'K':'¥'+v.toLocaleString('en-US');ctx.render(h('div',null,h('div',{style:{display:'flex',justifyContent:'space-between',fontSize:12}},h('span',{style:{color,fontWeight:500}},fmt(actual)),h('span',{style:{color:'#8c8c8c'}},'/ '+fmt(target))),h('div',{style:{height:4,background:'#f0f0f0',borderRadius:2,marginTop:3,overflow:'hidden'}},h('div',{style:{width:Math.min(pct,100)+'%',height:'100%',background:color,borderRadius:2}}))));
