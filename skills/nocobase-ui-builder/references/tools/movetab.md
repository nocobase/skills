# `movetab`

对应 tool：

- `mcp__nocobase__PostFlowsurfaces_movetab`

## 用途

调整同一 page 下 tab 的顺序。

## 关键参数

- `sourceTabSchemaUid`
- `targetTabSchemaUid`
- `position`

## 规则

- 只支持同一 page 下的兄弟 tab。
- `position` 只用 `before` 或 `after`。
