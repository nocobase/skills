# `addaction`

对应 tool：

- `mcp__nocobase__PostFlowsurfaces_addaction`

## 用途

追加一个非 record action。

## 适用 scope

- `block`
- `form`
- `filterForm`
- `actionPanel`

## 规则

- 如果是单条记录动作，不要用它。
- action 类型必须是当前容器支持的公开 action key。
