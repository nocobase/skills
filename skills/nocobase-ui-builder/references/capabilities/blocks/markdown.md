# Markdown Block

`markdown` 用于静态说明、操作指南和页面文案。

## 适用场景

- 帮助文案
- 页面说明
- 运营公告

## 必备输入

不需要 collection resource。

## 高频配置

- `content`
- `title`
- `displayTitle`

## 关键约束

- 默认用它表达静态文本，不要为了简单文案启用 `jsBlock`。
- 公开简单改配基本只有 `content`。
