interface:
  display_name: "NocoBase UI Builder"
  short_description: "Route Modern page work through flow-surfaces Hosts/UI and run-js-sources Inline Workspaces"
  default_prompt: "Routes Host/UI=`nb api flow-surfaces`; Workspace=`nb api run-js-sources`. Intent-firstwholePage1 tabcomplex multi-blocknested popupmulti-reactionreadsmetadataokapply-blueprintlocalized editsbackend actionsget-reaction-metafirst writeapplyBlueprintno createMenu/createPage/compose/configure/updateSettings/add*/move*/remove*/set*Rulesfail->repair aggregate errors+retry<=5success only explicit local/live gapreportRepeat-eligiblecontextual list-templateskeyword-only searchdiscovery-onlylocal customizationtemplate sourcefirst resultpage wording≠local-only intentask, not copysame-titlerouteIdpageSchemaUidsame-groupsame-titlereplacedifferent-groupsame-titlenomergesemantic AntDesign iconmulti-non-filter keyed layout/titlestemplates exemptsingle non-filteroptionalunless askedhostBound搜索/filtersameHostfilterActionoptionalsetLayoutstring[][]number[][[a],[b]]2col[[a,b]]stackuid not keysearchPage≠filterfilter/search blockfilterForm4+ fieldscollapserow1defaultFilteromitbackend4explicitemptyinvalid<4errorsraw payload onlytreeTableomitrecordActionsdefaultaddChildEFmoveNoInjectfirstlivedirectnon-assoctitleField>name>code>title分析看板KPIJSBlocktrenddistributionrankingpercentage占比 chartrequiredtable/list!=chartchartassets.charts+block.chartcardGridban(top-level code/version,changes.settings,stepParams禁,propsdecoratorPropsflowRegistry)typejsBlockonlykanban/pipeline/status columnsKanbanassociatedRecords+associationField1 editFormtryTemplatesaveAsTemplate"

# Long-form guardrails kept here for docs-consistency checks and human review:
# rebuild defaults.collections from scratch via live metadata
# defaults.collections fieldGroups >10 effective fields
# defaults fieldGroups self-review approve/regenerate once lowest reasoning no-think no chain-of-thought
# fixed view/addNew/edit
# table addNew threshold
# associations popups{name,description} first-segment
# described generated add/edit fields => structured formBehavior or formBehaviorDescriptionReview.fields.<field>{decision,reasonCode}; no legacy review/empty-object fallback
# Create JS page and a new complete JS Block: flow-surfaces Host -> sourceMode:inline -> nb api run-js-sources open -> Settings Pass -> full Workspace edit -> compile-preview -> full snapshot save
# Complete Workspace compile failures stay on run-js-sources; compatibility single-file JSBlock uses settings.code or configure changes.code only after the capability gate.
# Explicit Light Extension externalization uses nb light; ordinary Inline Workspace does not.
# Save uses baseCommitId + baseOwnerFingerprint; 409 recovery is openLatest -> merge -> compilePreview -> save, never token replacement alone
# Settings schema is src/client/entry.json; normally 2-5 meaningful settings and at least two, with the four documented exceptions; code consumes ctx.settings; Host values do not create source commits
# Existing owners keep the compatibility gate; externalize only for explicit light-plugin, cross-Host reuse, independent Git, or distribution intent; unnamed destination uses the application default Repository
# Host Preview is a non-goal

dependencies:
  tools:
    - type: "cli"
      value: "nb"
      description: "Use nb api flow-surfaces for Host/UI operations and nb api run-js-sources for complete Inline Workspace source"

policy:
  allow_implicit_invocation: true
