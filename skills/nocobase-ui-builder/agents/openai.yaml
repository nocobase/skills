interface:
  display_name: "NocoBase UI Builder"
  short_description: "Route Modern page work by intent: whole-page blueprint, localized flow-surfaces edit, or reaction authoring"
  default_prompt: "Front door: `nb api flow-surfaces`Intent-firstwholePage1 tabcomplex multi-blocknested popupmulti-reactionapply-blueprintlocalized editsbackend actionsget-reaction-metafirst writeapplyBlueprintreadsmetadataokno createMenu/createPage/compose/configure/updateSettings/add*/move*/remove*/set*Rulesfail->repair aggregate errors+retry<=5after success only explicit local/live gap repaireportRepeat-eligiblecontextual list-templateskeyword-only searchdiscovery-onlylocal customizationtemplate sourcefirst resultpage wording≠local-only intentask, not copysame-titlerouteIdpageSchemaUidsame-groupsame-titlereplacedifferent-groupsame-titlenomergesemantic AntDesign iconmulti-non-filter keyed layout/titlestemplates exemptsingle non-filteroptionalunless askedfieldsLayout2up hostBound搜索/filtersameHostfilterActionoptionalsetLayoutstring[][]number[][[a],[b]]2col[[a,b]]stackuid not keysearchPage≠filterfilter/search block filterForm4+ fieldscollapserow1defaultFilteromitbackend4explicitemptyinvalid<4errorsraw payload onlytreeTableomitrecordActionsdefaultaddChildEFmoveNoInjectfirstlivedirectnon-assoctitleField>name>code>title分析看板trend/chartKPIJSBlockcardGridsettings.codeconfigurechanges.code/versionban(top-level code/version,changes.settings,stepParams禁,props/decoratorProps/flowRegistry)typejsBlockonlykanban/pipeline/status columnsKanbanassociatedRecords+associationField1 editFormtryTemplatesaveAsTemplate"

# Long-form guardrails kept here for docs-consistency checks and human review:
# rebuild defaults.collections from scratch via live metadata
# defaults.collections fieldGroups >10 effective fields
# defaults fieldGroups self-review approve/regenerate once lowest reasoning no-think no chain-of-thought
# fixed view/addNew/edit
# table addNew threshold
# associations popups{name,description} first-segment
# described generated add/edit fields => structured formBehavior or formBehaviorDescriptionReview{fields,hasTried:true}; avoid empty-object no-op fallback

dependencies:
  tools:
    - type: "cli"
      value: "nb"
      description: "Use nb api flow-surfaces for Modern-page UI operations"

policy:
  allow_implicit_invocation: true
