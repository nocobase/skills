interface:
  display_name: "NocoBase UI Builder"
  short_description: "搭建或修改 Modern page(v2) 的 page/tab/popup UI surface"
  default_prompt: "Use $nocobase-ui-builder for NocoBase Modern page (v2) UI surface work. Treat live get/catalog/readback as the truth and use the canonical runtime docs for lifecycle, target, and request-shape decisions."

policy:
  allow_implicit_invocation: true
