interface:
  display_name: "NocoBase UI Builder"
  short_description: "Build and modify NocoBase Modern page v2 pages, blocks, and structured validation flows"
  default_prompt: "Use $nocobase-ui-builder to create or update a NocoBase Modern page v2, route the task through the canonical references for page lifecycle or block mutations, and only enter validation, review, or smoke when I explicitly ask for it."
