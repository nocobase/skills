interface:
  display_name: "NocoBase UI Builder"
  short_description: "Route Modern page work by intent: whole-page blueprint, localized flow-surfaces edit, or reaction authoring"
  default_prompt: "Front door: `nb api flow-surfaces`Intent-firstwholePage1 tab complex/nested/reaction=>apply-blueprintlocalized edits backend actionsget-reaction-meta first write applyBlueprintreads okno createMenu/createPage/compose/configure/updateSettings/add*/move*/remove*/set*Rulesfail->repair aggregate errors+retry<=5success stop except explicit gap repairreportRepeat-eligible contextual list-templateskeyword-only search=discovery-only local customization backend orderpageword≠local ask.same-title routeId pageSchemaUidsame-group same-title replacedifferent-group same-title no mergenav.group/item AntDesign icon multi-non-filter keyed layout/titles; single optional hostBound搜索/filter sameHost filterAction optional setLayout string[][]number[][[a],[b]]=2col[[a,b]]=stack uid not key.searchPage≠filter filter/search block filterForm4+ fields collapse row1.defaultFilter omit backend4 explicit empty invalid<4 errors raw payload only 分析看板trend/chart KPIJSBlock cardGrid jsBlock settings.code/version assets.scripts+script configure changes.code/version ban top-level code/version changes.settings changes.script stepParams props decoratorProps flowRegistry type jsBlock only Never kanban/pipeline/status columns Kanban associatedRecords+associationField1 editForm tryTemplate saveAsTemplate"

# Long-form guardrails kept here for docs-consistency checks and human review:
# rebuild defaults.collections from scratch via live metadata
# defaults.collections fieldGroups >10 effective fields
# defaults fieldGroups self-review approve/regenerate once lowest reasoning no-think no chain-of-thought
# fixed view/addNew/edit
# table addNew threshold
# associations popups{name,description} first-segment
# described generated add/edit fields => structured formBehavior or formBehaviorDescriptionReview{fields,hasTried:true}; avoid empty-object no-op fallback

dependencies:
  tools:
    - type: "cli"
      value: "nb"
      description: "Use nb api flow-surfaces for Modern-page UI operations"

policy:
  allow_implicit_invocation: true
