interface:
  display_name: "NocoBase UI Builder"
  short_description: "Route Modern page work by intent: whole-page blueprint, localized flow-surfaces edit, or reaction authoring"
  default_prompt: "Canonical front door: `nocobase-ctl flow-surfaces`.Intent-first:apply-blueprint;localized edits `flow-surfaces`;get-reaction-meta.wholePage=1 route tab|complex multi-block|nested popup|multi-reaction.first write=applyBlueprint;reads/metadata/preview/prepare-write ok;no createMenu/createPage/compose/configure/updateSettings/add*/move*/remove*/set*Rules;fail->stop+report;after success only explicit local/live gap repair.Repeat-eligible must contextual list-templates;keyword-only search discovery-only;first result.template source;page wording≠local-only intent;local customization->copy;ask, not copy.same-title->routeId;routeId->pageSchemaUid;navigation.group+navigation.item AntDesign icon.multi-non-filter explicit keyed layout.hostBound搜索/filter;sameHost filterAction;searchPage≠filter;explicit filter/search block->filterForm;filter row 1;filterForm 4+ fields->collapse.rebuild defaults.collections live metadata;fieldGroups>10;fixed view/addNew/edit;table addNew threshold;popups{name,description};associations first-segment.field-popup;associatedRecords+associationField;1 editForm.popup.tryTemplate,popup.saveAsTemplate,openView.tryTemplate"

dependencies:
  tools:
    - type: "mcp"
      value: "nocobase"
      description: "NocoBase MCP server remains the backend contract source and fallback path for Modern-page UI operations"

policy:
  allow_implicit_invocation: true
