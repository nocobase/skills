[
  {
    "id": "case-17",
    "request": "Several compatible Hosts must execute one shared JavaScript implementation. Maintainers should update that implementation once and have every bound Host use the new code.",
    "expectedRoute": "js-template"
  },
  {
    "id": "case-42",
    "request": "Create a JS Block whose implementation belongs only to that one Host. No other Host should share or update the same JavaScript, and no backend capability is needed.",
    "expectedRoute": "inline-runjs"
  },
  {
    "id": "case-08",
    "request": "This custom JS feature cannot be delivered client-side: implement a new server API and service, a persistence table and migration, and feature-specific ACL enforcement.",
    "expectedRoute": "nocobase-plugin"
  },
  {
    "id": "case-31",
    "request": "Reuse the same table, form, popup, and Flow layout on several pages, but let every page keep its own behavior and JavaScript. There is no shared JS implementation and no backend work.",
    "expectedRoute": "ui-template"
  },
  {
    "id": "case-64",
    "request": "Several Hosts should share one JavaScript implementation, but the feature also requires implementing a new server API, database table, migration, and feature-specific ACL enforcement.",
    "expectedRoute": "nocobase-plugin"
  },
  {
    "id": "case-05",
    "request": "One Host exclusively owns this JavaScript implementation. Keep its source in an independent Git repository for that Host, but do not share the implementation and do not add backend capability.",
    "expectedRoute": "inline-runjs"
  }
]
